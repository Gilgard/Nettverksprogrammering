// needs to be able to decrypt packages and pass it on:


// if its the end node recieving data from a third party it needs to be able to encrypt with layers:
// get request sent to third parties should probably wait for set up a thread that waits for reply, 
// finds a path back to source and encrypts.